# kestrel-talk

## Rules

- **Before every `git push`**: Run full local browser E2E testing — start mock server + dev server, verify all features work in browser (connection, messaging, streaming, code blocks, search, export, mobile view). Do not push untested code.
- Only write code in the kestrel-talk project. Read-only access to kestrel-agent.
